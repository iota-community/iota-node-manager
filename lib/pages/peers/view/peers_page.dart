import 'package:flutter/material.dart';

class PeersPage extends StatelessWidget {
  const PeersPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Peers');
  }
}
